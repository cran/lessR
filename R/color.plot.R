color.plot <-
function(...) {

    cat("\n"); cat(  # stop(call.=FALSE, "\n","------\n",
      "The lessR project is now much more than just about color.\n",
      "Accordingly, the function  color.plot has been renamed.\n\n",
      "Now use  plt  instead of  color.plot.\n\n")

    plt(...)

    cat("\n"); cat(   # stop(call.=FALSE, "\n","------\n",
      "The lessR project is now much more than just about color.\n",
      "Accordingly, the function  color.plot has been renamed.\n\n",
      "Now use  plt  instead of  color.plot.\n\n")

}
