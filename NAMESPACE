useDynLib(lessR)

importFrom("car", vif, scatter3d, dataEllipse)
importFrom("leaps", leaps)
importFrom("MBESS", ci.smd)
importFrom("foreign", read.spss)
importFrom("triangle", dtriangle, rtriangle)

# Export all names except those that begin with .
#exportPattern("^[^\\.]")
#
# Export all names
#exportPattern(".")


export (

  ANOVA, av, av.brief,
  BarChart, bc,
  BoxPlot, bx,
  Correlation, cr, cr.brief,
  CountAll, ca,
  corCFA, cfa, scales,
  corReorder, reord, 
  corReflect, reflect, corProp, prop, corRead, rad.cor, 
  corScree, scree, corEFA, efa,
  Density, dn, den,
  Help, hlp,
  Histogram, hs, hst,
  label,
  LineChart, lc,
  Logit, lgt,
  Model, model, model.brief,
  PieChart, pc,
  prob.norm, prob.znorm, prob.tcut,
  Read, rad, rad2, rad.brief, rad.labels, rad.both, 
  Regression, reg, reg.brief, reg.explain,
  Recode, rec,
  set, showColors,
  simCImean, simCLT, simFlips, simMeans,
  ScatterPlot, sp, Plot, DotPlot, dp,
  Sort, srt,
  Subset, subs, locate,
  SummaryStats, ss, ss.brief,
  to,
  Transform, trans,
  ttest, tt, tt.brief, smd.t.test,
  ttestPower, ttp,
  values, 
  Write, wrt, wrt.r
)

S3method(bc, default)
S3method(bc, data.frame)
S3method(bx, default)
S3method(bx, data.frame)
S3method(hst, default)
S3method(hst, data.frame)
